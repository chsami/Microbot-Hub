package net.runelite.client.plugins.microbot.actions.fixtures.empty;

/**
 * An unrelated top-level class sharing the {@link EmptyScanTarget} package. Discovery must ignore it
 * (it does not implement the scanned type), proving the scan is predictable and type-filtered.
 */
public class NotAnAction {
}
